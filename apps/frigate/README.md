# Frigate

## Setup

To install or update, run:

```bash
bash -c "$(wget -qO - https://raw.githubusercontent.com/jannoelc/home-server/develop/lxc/frigate/install.sh)"
```

Start using the command below:

```bash
docker compose up -d
```
